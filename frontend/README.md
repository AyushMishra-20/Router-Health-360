# Frontend

Owned by: Frontend teammate.

Scaffold with Vite (`npm create vite@latest . -- --template react`) or plain HTML/JS if
speed matters more than framework polish. Build against `docs/api-contract.md`.
